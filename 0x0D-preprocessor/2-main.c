#include <stdio.h>

/**
 * main - Prints the name of the file the program was compiled 
 * from followed by a ew line.
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("%s\n", _FILE_);

	return (0);
}
